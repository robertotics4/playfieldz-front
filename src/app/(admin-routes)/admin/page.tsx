import { ProfileHeader } from '@/app/components/ProfileHeader'

export default async function Admin() {
  return (
    <main className="flex flex-col p-8 bg">
      <ProfileHeader />
    </main>
  )
}
